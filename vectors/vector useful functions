#include <iostream>
#include <vector>
#include <bits/stdc++.h>
using namespace std;

int main()
{   int n ;
    cout<<"please enter the size"<<endl ;
    cin>>n ;
    vector<int> a(5,n) ;
    vector<int> b(a.begin(),a.end()) ;
    vector<int> c{1,2,3,4,5} ;
    
            
    // for (int i = 0; i < a.size() ; i++)
    // {
    //     cout<<a[i]<<" " ;
        
    // }
    // cout<<endl ;
    vector<int> v ;
    for (int i = 0; i < n; i++)
    {
        int no ;
        cin>>no ;
        v.push_back(no) ;
    }
    int arr[] = {1,2,3,4} ;
    cout<<v.max_size()<<endl ;
    cout<<v.capacity()<<endl ;
    cout<<v.size()<<endl ;
    int dd ;
    cout<<"enter resize"<<endl ;
    cin>>dd ;
    v.resize(dd) ;
    // v.pop_back() ;
    // v.insert(v.begin()+3 , 100) ;
    // to erase the element 
    // v.erase(v.begin()+3);
    // v.erase(v.begin()+2, v.begin()+5);

    v.reserve(100) ; 
    for (int i = 0; i < n ; i++)
    {
        cout<<v[i]<<" " ;
        
    }

    cout<<v.max_size()<<endl ;
    cout<<v.capacity()<<endl ;
    cout<<v.size()<<endl ;
    
     vector< vector< int > > matrix(2, vector < int > (3, 100) ) ;

  
    for(int i=0; i<3;i++){
        for (int j = 0; j < 3; j++)
        {
         
        cout<<matrix[i][j]<<" " ;
        }
        cout<<endl ;
    }
    
    return 0;
}
